{
  "name": "vararu.org",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "next build && next export",
    "deploy": "./deploy.sh",
    "dev": "next",
    "lint-staged": "lint-staged",
    "lint": "echo \"Forget about flow for now.\"",
    "predeploy": "npm run build",
    "start": "serve out --open --port=$PORT",
    "test": "npm run lint"
  },
  "dependencies": {
    "babel-preset-flow": "^6.23.0",
    "classnames": "^2.2.5",
    "next": "^4.0.3",
    "raf-throttle": "^2.0.2",
    "react": "^16.7.0",
    "react-dom": "^16.0.0",
    "react-motion": "^0.5.0",
    "react-no-ssr": "^1.1.0",
    "serve": "^10.0.0"
  },
  "devDependencies": {
    "husky": "^0.14.0",
    "npm-run-all": "^4.0.2",
    "prettier": "^1.7.4",
    "webpack-visualizer-plugin": "^0.1.11"
  }
}
