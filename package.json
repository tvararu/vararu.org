{
  "name": "vararu.org",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "next build && next export",
    "deploy": "./deploy.sh",
    "dev": "next",
    "flow-coverage": "flow-coverage-report -i 'pages/**/*.js' -i 'components/**/*.js' -t html -o coverage/flow-coverage-report",
    "flow": "flow",
    "lint-staged": "lint-staged",
    "lint": "flow check",
    "predeploy": "npm run build",
    "start": "serve out --open --port=$PORT",
    "test": "npm run lint"
  },
  "dependencies": {
    "babel-preset-flow": "^6.23.0",
    "classnames": "^2.2.5",
    "next": "^4.0.3",
    "raf-throttle": "^2.0.2",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-motion": "^0.5.0",
    "react-no-ssr": "^1.1.0",
    "serve": "^7.0.0"
  },
  "devDependencies": {
    "flow-bin": "^0.52.0",
    "flow-coverage-report": "^0.3.0",
    "husky": "^0.14.0",
    "npm-run-all": "^4.0.2",
    "prettier": "^1.7.4",
    "webpack-visualizer-plugin": "^0.1.11"
  }
}
